//! `SeaORM` Entity, @generated by sea-orm-codegen 2.0

use std::{fmt::Display, str::FromStr};

use sea_orm::{entity::prelude::*, sea_query::ValueTypeErr, sqlx::types::chrono::Utc, ActiveValue::Set};
use serde::{Deserialize, Serialize};

use crate::user_relations_userrelation::UserRelationId;

#[sea_orm::model]
#[derive(Clone, Debug, PartialEq, Eq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "diaries_diary")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false)]
    pub id: Uuid,
    #[sea_orm(column_type = "Text")]
    pub entry: String,
    pub date: Date,
    pub created_at: DateTimeWithTimeZone,
    pub updated_at: DateTimeWithTimeZone,
    pub user_relation_id: UserRelationId,
    pub user_1_status: DiaryStatus,
    pub user_2_status: DiaryStatus,
    #[sea_orm(has_many, via = "diaries_diarytagrelation")]
    pub tags: HasMany<super::diaries_diarytag::Entity>,
    #[sea_orm(belongs_to, from = "user_relation_id", to = "id", on_update = "NoAction", on_delete = "NoAction")]
    pub user_relations_userrelation: HasOne<super::user_relations_userrelation::Entity>,
}

#[derive(DeriveValueType, Clone, Debug, PartialEq, Eq, Serialize, Deserialize, Copy)]
#[sea_orm(value_type = "String", column_type = "String(StringLen::N(8))")]
pub enum DiaryStatus {
    Unread,
    Read,
    Edited,
}
impl FromStr for DiaryStatus {
    type Err = ValueTypeErr;
    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s {
            "unread" => Ok(Self::Unread),
            "read" => Ok(Self::Read),
            "edited" => Ok(Self::Edited),
            // NOTE: Invalid status should fall back to unread so that after reading, it will safely be turned to read.
            _ => Ok(Self::Unread),
        }
    }
}
impl Display for DiaryStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        write!(
            f,
            "{}",
            match self {
                Self::Unread => "unread",
                Self::Read => "read",
                Self::Edited => "edited",
            }
        )
    }
}
impl Default for DiaryStatus {
    fn default() -> Self {
        Self::Unread
    }
}

impl ActiveModelBehavior for ActiveModel {
    /// Create a new ActiveModel with default values. Also used by `Default::default()`.
    fn new() -> Self {
        Self {
            id: Set(Uuid::now_v7()),
            created_at: Set(Utc::now().into()),
            updated_at: Set(Utc::now().into()),
            ..ActiveModelTrait::default()
        }
    }
}
