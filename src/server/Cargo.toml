[package]
name = "server"
version = "0.1.0"
edition = "2021"

[lib]
name = "server"
path = "lib.rs"

[dependencies]
general = { path = "../general" }
user = { path = "../user" }

actix-session.workspace = true
actix-web.workspace = true
