[package]
name = "server"
version = "0.1.0"
edition = "2021"

[lib]
name = "server"
path = "lib.rs"

[dependencies]
common = { path = "../common", features = ["settings"] }
user = { path = "../user" }
user_relation = { path = "../user_relation" }
ticket = { path = "../ticket" }
diary = { path = "../diary" }
diary_tag = { path = "../diary_tag" }
web_push_subscription = { path = "../web_push_subscription" }

actix-session.workspace = true
actix-web.workspace = true
