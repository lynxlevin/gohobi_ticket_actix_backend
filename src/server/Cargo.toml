[package]
name = "server"
version = "0.1.0"
edition = "2021"

[lib]
name = "server"
path = "lib.rs"

[dependencies]
general = { path = "../general", features = ["settings"] }
user = { path = "../user" }

actix-session.workspace = true
actix-web.workspace = true
